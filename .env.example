# Шаблон переменных окружения
# Копируйте этот файл в .env и добавьте реальные значения
# НЕ добавляйте .env в гит - он бывает в .gitignore

# Telegram Bot
BOT_TOKEN=your_telegram_bot_token_here
CHAT_ID=your_chat_id_here

# Database (optional)
DB_HOST=localhost
DB_PORT=5432
DB_USER=admin
DB_PASSWORD=your_secure_password
DB_NAME=your_database_name

# API Keys (if using external APIs)
API_KEY_EXTERNAL=your_api_key_here

# Application Settings
NODE_ENV=development
PORT=3000
DEBUG=false

# IMPORTANT SECURITY NOTES:
# 1. Никогда не накладывайте реальные токены в .env.example
# 2. Обсолютно .env в .gitignore
# 3. Используйте GitHub Secrets для CI/CD
# 4. Периодически ротируйте токены
